#include<bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    int w[n], p[n];
    memset(w, 0, n);
    memset(p, 0, n);
    for(int i=0; i<n; i++)
        cin>>w[i]>>p[i];
    

    return 0;
}